# Telegram Bot Token (get from BotFather)
BOT_TOKEN=your_bot_token_here

# Deployment environment: 'polling' or 'webhook'
ENVIRONMENT=polling

# For webhook deployment (Fly.io)
# WEBHOOK_URL=https://your-app-name.fly.dev

# Server port (default: 3000)
PORT=3000
